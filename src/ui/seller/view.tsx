import { SellerPropsModel } from "../../models/seller/types/seller-props-model";

export default async function SellerView({
  celular,
  cnpj,
  email,
  id,
  nome,
  senha,
  status,
}: SellerPropsModel) {
  return <div>Tetse</div>;
}
